description=resource metrics, for kubectl top and autoscaling
version=v0.7.2
namespace=kube-system
# The aggregated API answering is the point, not the pod being Ready: the API
# server has to reach metrics-server, and metrics-server has to have scraped.
check=kubectl top nodes
