description=image registry at localhost:5001, backed by a PersistentVolume
version=3.1.1
namespace=registry
# The pod being Ready says the registry answers inside its VM; this says the
# hostPort on the Mac reaches it, which is the address everything uses.
check=curl -fsS --max-time 2 http://localhost:5001/v2/
