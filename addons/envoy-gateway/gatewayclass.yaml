# The class a Gateway names to be served by this controller. Upstream leaves it
# to its quickstart; a controller with nothing to claim is not much of an addon.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
